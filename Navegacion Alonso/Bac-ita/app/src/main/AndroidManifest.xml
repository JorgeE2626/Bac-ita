<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Navegacion">

        <!-- 🔥 AHORA INICIA EN AVISO -->
        <activity
            android:name=".AvisoActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- DEMÁS PANTALLAS -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".RecuperarActivity" />
        <activity android:name=".VerificacionActivity" />
        <activity android:name=".MainActivity" /><activity android:name=".ui.theme.RegistroRanchoActivity"/>
        <activity android:name=".ui.theme.RegistroGanadoActivity"/>
        <activity android:name=".ui.theme.ActualizarDatosActivity"/>

    </application>

</manifest>